# 2026-08-04 final polish pass

## Summary

Production `https://certamaris.com/legal/privacy`, `/legal/terms`, and
`/legal/dpa` were returning `404` because the live `certamaris-site`
Cloudflare Worker had been overwritten by the monorepo `apps/marketing`
snapshot. The canonical `certamaris-public-website` repo already contained
the legal routes, so the corrective action was to rebuild and redeploy from
this repo only.

The live legal-route incident is fixed. The site was redeployed from
`C:\certamaris-startup-site-pnpm\certamaris-startup-site` and verified live
at 2026-08-04T23:00:05Z.

## Root cause

- Correct source repo: `marinerxcapital/certamaris-public-website`.
- Correct local deploy path:
  `C:\certamaris-startup-site-pnpm\certamaris-startup-site`.
- Bad live source before this pass: monorepo `apps/marketing` snapshot.
- Collision: the monorepo marketing snapshot had previously shared the real
  production Worker name, `certamaris-site`.
- Broken live version before fix:
  `88f7dc91-ab7e-4280-8fd8-fc98fb0fc4ea`, deployed 2026-08-04T05:31:40Z
  with message `canonical monorepo marketing cutover 4a872f8`.

## What shipped

- Redeployed the canonical static export to `certamaris-site`.
- Fixed the SEO regression found during QA by giving legacy compatibility
  routes distinct metadata:
  - `/privacy` now uses `Privacy Policy Status`.
  - `/terms` now uses `Business Terms Status`.
- Hardened `scripts/qa/audit-crawl.mjs` so the documented full-route crawl is
  reproducible after a clean install:
  - declared `playwright-core` and `axe-core` as dev dependencies;
  - mirrored the Worker `_rsc` route rewrite in the local static audit server;
  - added a local `/api/status` fixture for the status page;
  - writes results before browser cleanup and exits cleanly if Chrome cleanup
    stalls.

## Validation results

### Build and repository QA

- `npm install` - PASS; reported existing audit state `3 vulnerabilities`
  (`2 moderate`, `1 high`); no forced dependency upgrade was applied.
- `npm run typecheck` - PASS.
- `npm run build` - PASS; 105 static pages generated by Next.
- `npm run build:static` - PASS; 105 static pages generated and `out/`
  refreshed.
- `npm run qa` - PASS:
  - contact and Worker tests: `12/12` pass;
  - content QA: `112` files clean;
  - route export check: `96` pass, `0` fail;
  - SEO check: `99` pages pass; expected warnings only for shared 404 and
    legacy legal-status H1 compatibility pages;
  - internal link check: `7,553` internal hrefs resolved;
  - founder QA: `11/11` pass.

### Full accessibility and overflow crawl

Command:

```powershell
$env:CHROMIUM_PATH='C:\Program Files\Google\Chrome\Application\chrome.exe'
node scripts/qa/audit-crawl.mjs 2026-08-04-final-polish --no-shots
```

Result:

- Sitemap routes crawled: `91`.
- Result rows: `182` (`91` routes x desktop/mobile).
- Axe violation pages: `0`.
- Horizontal overflow pages: `0`.
- Desktop console errors: `0`.
- Mobile console errors: `0`.
- Output: `audit/2026-08-04-final-polish/results.json`.

### Live legal-route verification

Verified twice after deploy. Each remote body hash matched the local
`out/legal/*.html` hash exactly.

| Route | HTTP | SHA-256 |
|---|---:|---|
| `/legal/privacy` | `200` | `B5266982A1342D64DC038BADA38B86292A61E6363F86C5BEBCB551AC046F0509` |
| `/legal/terms` | `200` | `9842F6EE35C8070B86A78BAA2240D20BBFFC3F0A045A3C1F8B2B2976BD03027C` |
| `/legal/dpa` | `200` | `1CB831A14FEE4456B303F6628223CDD3C3A3AECBAB1FA4A8EB2D14594412353C` |

### Live route and asset spot checks

All returned `200`:

- `/`
- `/platform`
- `/platform/evidence`
- `/solutions/audit-survey-readiness`
- `/solutions/imo-msc-428-98`
- `/who-we-serve/ship-owners`
- `/who-we-serve/technical-managers-dpas`
- `/pricing`
- `/trust/status`
- `/trust/procurement`
- `/contact`
- `/demo`
- `/resources/imo-msc-428-98-explained`
- `/about/leadership`
- `/compliance/iacs`
- `/topics/iacs-ur-e26`
- `/robots.txt`
- `/favicon.ico`
- `/apple-icon.png`
- `/icon.png`
- `/og/certamaris-og.jpg`
- `/brand/certamaris-full.png`
- `/api/status`

Styled 404 check:

- `/not-a-real-route-codex-20260804` returned `404` with title
  `Page not found - CertaMaris` and H1
  `This page isn't on the chart.`

### Craft and interaction checks

- Live screenshot review covered homepage, demo, contact, and legal-status
  pages at desktop, `390px`, and `375px`.
- `node scripts/qa/check-demo-images.mjs --base https://certamaris.com ...`
  verified all sampled homepage/demo/platform/solution/resource/leadership
  and procurement images loaded.
- `node scripts/qa/check-sample-record.mjs --base https://certamaris.com`
  passed tab click, keyboard navigation, linked-record jump, and focus move
  checks.
- `node scripts/qa/perf-hero.mjs` ran in normal and reduced-motion modes:
  - normal: `load=4287ms`, `cells=0`, median frame `33.3ms`, p95 `34.2ms`;
  - reduced motion: `load=1894ms`, `cells=0`, median frame `33.3ms`, p95
    `33.4ms`.

## Contact delivery status

Still not configured live.

- `npx wrangler secret list --config wrangler.jsonc` returned `[]`.
- Live synthetic JSON POST to `https://certamaris.com/api/contact` returned
  `503` with the expected fail-closed fallback message:
  `Delivery failed. Please email skyler@certamaris.com or sales@certamaris.com with the same details.`
- Request ID observed: `73b974aa-e829-4e93-aec0-000ea068d82a`.

This remains a configuration/provisioning blocker. No secret, endpoint, email
binding, provider credential, or destination was invented during this pass.

## Deployment

- Deploy command:

```powershell
npx wrangler deploy --config wrangler.jsonc --keep-vars
```

- Worker: `certamaris-site`.
- Corrective deployment created: 2026-08-04T22:59:04.541Z.
- Worker version:
  `6f4f4340-a5d0-4577-8c95-24260fb48010`.
- Uploaded assets: `504` new or modified; `526` already uploaded.

## Git

- `431be5bcf960e062eabbf179b3c97a308e2f38e5`
  `fix(marketing): distinguish legacy legal route metadata`
- `c4926e9`
  `test(marketing): harden full-route audit crawl`

## Known residuals

- Contact delivery is still not configured on the Worker.
- The pricing page still intentionally omits the finalized dollar amounts.
  Publishing those numbers remains a GTM/business decision, not a bug fix.
- The `/legal/*` pages intentionally remain status pages. The operative legal
  text is not published because the source package still has unresolved legal,
  commercial, privacy, security, and transfer fields.
- `npm install` reports `3` dependency audit findings (`2 moderate`, `1 high`);
  no forced breaking upgrade was applied during this production incident pass.
- The working tree had unrelated pre-existing dirty files before this pass
  (`components/DepthContours.tsx`, `components/HeroPixelGrid.tsx`, and
  temporary scratch files). They were not staged or modified by this pass.
